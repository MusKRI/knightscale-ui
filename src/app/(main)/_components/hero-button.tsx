"use client";

import Button from "@/core/components/ui/button/Button";

const HeroButton = () => {
  return ( 
    <Button variant="primary" className="mt-8 px-12 tracking-wide">Browse</Button>
   );
}
 
export default HeroButton;